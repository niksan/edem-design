# Use the README file as a reference to customize settings.

News:
  fields:
    default: title, date, body, permalink, portfolio_id
    form: title, date, body, permalink, portfolio_id
  relationships: 
  application: Application
