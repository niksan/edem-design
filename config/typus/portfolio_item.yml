# Use the README file as a reference to customize settings.

PortfolioItem:
  fields:
    default: title, portfolio, portfolio_item_image
    form: title, portfolio, portfolio_item_image
  relationships: 
  application: Application
