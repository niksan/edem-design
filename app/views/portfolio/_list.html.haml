#portfolio_items
  - items.each do |item|
    = link_to image_tag(item.portfolio_item_image.thumb, alt: item.title), item.portfolio_item_image.medium, title: item.title, rel: 'gallery', class: 'fancybox'
.clear
